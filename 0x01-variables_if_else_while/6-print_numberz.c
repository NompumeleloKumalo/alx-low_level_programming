#include <stdio.h>
/**
 * main - Prints all single digits from zero to nine
 * Return: 0
 */

int main(void)
{
	int p;

	p = 0;

	while

		(p < 10)

		{
			putchar(p + '0');
			p++;
		}
	putchar ('\n');
	return (0);
}
