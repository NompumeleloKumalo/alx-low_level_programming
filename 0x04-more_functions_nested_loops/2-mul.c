#include "main.h"
/**
 * mul - checks for a digit, 0 through 9
 * @a: a digit to be checked
 * @b: a digit to be checked
 * Return: 0
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
